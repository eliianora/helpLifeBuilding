import { t as AdminResourcePage } from "./admin-resource-page-B8vUzpyt.js";
import { jsx } from "react/jsx-runtime";
//#region src/routes/admin/paiements.tsx?tsr-split=component
function AdminPaiements() {
	return /* @__PURE__ */ jsx(AdminResourcePage, {
		resource: "paiements",
		title: "Paiements",
		description: "Consultez les transactions et corrigez leur statut.",
		allowCreate: false,
		allowDelete: false,
		editLabel: "Changer le statut",
		fields: [{
			key: "statut",
			label: "Statut",
			type: "select",
			required: true,
			options: [
				{
					label: "Payé",
					value: "paid"
				},
				{
					label: "En attente",
					value: "pending"
				},
				{
					label: "Échoué",
					value: "failed"
				},
				{
					label: "Remboursé",
					value: "refunded"
				}
			]
		}],
		columns: [
			{
				key: "date_paiement",
				label: "Date",
				format: (value) => value ? new Date(String(value)).toLocaleString("fr-FR") : "—"
			},
			{
				key: "montant",
				label: "Montant",
				format: (value, row) => `${Number(value ?? 0).toLocaleString("fr-FR")} ${row.devise ?? "XOF"}`
			},
			{
				key: "moyen",
				label: "Moyen"
			},
			{
				key: "statut",
				label: "Statut"
			},
			{
				key: "utilisateur_id",
				label: "Utilisateur"
			}
		]
	});
}
//#endregion
export { AdminPaiements as component };
